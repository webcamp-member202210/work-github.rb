<div class="container d-flex mt-4 mb-4">
  <h4 class="bg-light">注文履歴一覧</h4>
</div>
<div>
  <%= flash[:notice] %>
</div>

<div class="container" style="width: 60%">
  <div class="align-items-center justify-content-center">
    <div class="row">
      <table class="table">
        <thead>
          <tr class="thead-light">
            <th style="width: 20%">購入日時</th>
            <th style="width: 20%">購入者</th>
            <th style="width: 20%">注文個数</th>
            <th style="width: 20%">注文ステータス</th>
          </tr>
        </thead>
        <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td>
              <U>
                <%= link_to admin_order_path(order.id) do %>
                  <u class="text-dark"><%= l order.created_at, format: :default %></u>
                <% end %>
              </U>
            </td>
            <td><%= order.customer.last_name %><%= order.customer.first_name %></td>
            <td>
              <%= order.order_details.sum(:amount) %>
            </td>
            <td>
            <%= order.status_i18n %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>