<table>
  <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <tr>
    <% @genres.each do |genre| %>
      <td>
      <%= link_to public_items_path do %>
        <%= genre.name %>
      <% end %>
      </td>
    <% end %>
    </tr>
  </tbody>
</table>

<h2>
  ようこそ、ながのCAKEへ！<br>
  このサイトは、ケーキ販売のECサイトになります。<br>
  会員でない方も商品の閲覧はできますが、<br>
  購入には会員登録が必要になります。
</h2>

<h2>新着商品</h2>


<% @items.each do |item| %>
  <div>
    <%= link_to public_item_path(item.id) do %>
      <%= image_tag item.image(100, 100) %>
    <% end %>
  </div>
  <h2><%= item.name %></h2>
  <h2>¥<%= item.price.to_s(:delimited) %></h2>
